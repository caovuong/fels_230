<%= form_for current_user.active_relationships.build, remote: true do |f| %>
  <div>
    <%= hidden_field_tag :following_id, @user.id %>
  </div>
  <%= f.submit t("views.users.follow.bt_follow"), class: "btn btn-primary" %>
<% end %>

